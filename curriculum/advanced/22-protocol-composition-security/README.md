# 22 — Protocol Composition Security

Trace identity, authority, data, trust, policy, trace, and revocation across
User → UI → Orchestrator → A2A specialist → MCP → enterprise API. Demonstrate
authority confusion at a hop; create a hop ledger that rejects missing audience,
tenant, expiry, or delegation constraints. Measure lost-identity and scope-widening events.
