# 24 — Agent Security Testing and Fuzzing

Write unit, negative, contract, property, and fuzz tests for schemas, tool
arguments, paths, URLs, resource limits, and state transitions. With Hypothesis
or equivalent assert no crash, leakage, unauthorized action, or unbounded
resource use. Keep adversarial fixtures versioned and emit counterexamples
without secrets.
