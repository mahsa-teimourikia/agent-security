# 34 — Enterprise Agent Security Architecture

Design Users → Agent Gateway → Identity/Delegation → Policy → Runtime → Tool/MCP
Gateway → Enterprise Systems with context security, memory, sandbox, secrets,
egress, observability, evaluation, registry, governance, and incident response.
Compare centralized and federated control placement; evaluate enforcement,
telemetry, and recovery coverage at each boundary.
